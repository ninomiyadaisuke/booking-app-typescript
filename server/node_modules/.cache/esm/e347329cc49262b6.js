let express,readdirSync,cors,mongoose;_c49‍.w("express",[["default",["express"],function(v){express=v}]]);_c49‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_c49‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_c49‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

const app = express();

mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser:true,
    useFindAndModify:false,
    useUnifiedTopology:true,
    useCreateIndex
  })
  .then(() => console.log("DB Connected"))
  .catch(() => console.log("DB Connection Error"));

app.use(cors());
app.use(morgan("dev"));

readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));
